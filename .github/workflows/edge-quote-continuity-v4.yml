name: Edge quote continuity V4

on:
  push:
    paths:
      - '.deploy-edge-quote-continuity-v4'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Refine quote continuity V4
        run: python .github/scripts/refine-quote-continuity-v4.py

      - name: Commit refinement
        run: |
          rm -f .deploy-edge-quote-continuity-v4
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/lib/edge-omnivoice-inspired.ts .deploy-edge-quote-continuity-v4
          git commit -m "Refine direct-quote boundaries"
          git push
