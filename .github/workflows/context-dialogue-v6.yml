name: Context dialogue V6

on:
  push:
    paths:
      - '.deploy-context-dialogue-v6'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Refine V6 context dialogue
        run: python .github/scripts/refine-context-dialogue-v6.py

      - name: Commit refinement
        run: |
          rm -f .deploy-context-dialogue-v6
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/lib/edge-emotion-director.ts app/api/synthesize/route.ts .deploy-context-dialogue-v6
          git commit -m "Refine role inference and story semantic pauses"
          git push
