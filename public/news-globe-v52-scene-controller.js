(function boot(){const G=window.NG14;if(!G||!window.Cesium||!G.viewer||!G.countries)return setTimeout(boot,80);if(G.__v52AuthoritativeController)return;G.__v52AuthoritativeController=true;const C=Cesium,legacy=G.runSequence,ents=[];const good=(a,b)=>Number.isFinite(+a)&&Number.isFinite(+b),wait=(ms,s)=>G.wait?G.wait(ms,s):new Promise(r=>setTimeout(()=>r(s===G.navSerial),ms));
const ISO2={RUS:'ru',UKR:'ua',SAU:'sa',YEM:'ye',USA:'us',IRN:'ir',OMN:'om',CHN:'cn',FRA:'fr',CHE:'ch'};
function rm(e){try{G.viewer.entities.remove(e)}catch{}}function clear(){for(const e of ents.splice(0))rm(e);for(const k of ['v51SceneEntities','v50Entities','v49Entities','v48Entities','v47Entities','v45bEntities','v44Entities','v38Entities','v37Entities','v36Entities']){const a=G[k];if(Array.isArray(a))for(const e of a.splice(0))rm(e)}try{G.clearInteractionEffects?.()}catch{}try{G.clearSecondaryCountry?.()}catch{}try{G.clearLocal?.()}catch{}try{G.clearCountry?.()}catch{}try{G.clearArc?.()}catch{}try{if(G.markers)for(const e of G.markers)e.show=false;if(G.pulses)for(const e of G.pulses)e.show=false}catch{}}
function feature(iso){return G.countries.get(String(iso||'').toUpperCase())?.feature||null}function collect(x,o=[]){if(!x)return o;if(Array.isArray(x)){if(x.length>1&&typeof x[0]==='number'&&typeof x[1]==='number')o.push(x);else for(const y of x)collect(y,o)}else if(x.coordinates)collect(x.coordinates,o);else if(x.geometry)collect(x.geometry,o);else if(x.features)for(const y of x.features)collect(y,o);return o}function carts(iso){return collect(feature(iso),[]).filter(p=>good(p[0],p[1])).map(p=>C.Cartesian3.fromDegrees(+p[0],+p[1],0))}
function polygons(g){if(!g)return[];if(g.type==='Feature')return polygons(g.geometry);if(g.type==='FeatureCollection')return(g.features||[]).flatMap(x=>polygons(x));if(g.type==='Polygon')return[g.coordinates];if(g.type==='MultiPolygon')return g.coordinates||[];return[]}
function area(r){let a=0;for(let i=0,j=r.length-1;i<r.length;j=i++)a+=(+r[j][0])*(+r[i][1])-(+r[i][0])*(+r[j][1]);return Math.abs(a/2)}
function mainRing(iso){let best=null,ba=-1;for(const p of polygons(feature(iso))){const r=p?.[0]||[],a=area(r);if(r.length>3&&a>ba){best=r;ba=a}}return best}
function centroid(r){let x=0,y=0,n=0;for(const p of r||[]){if(good(p?.[0],p?.[1])){x+=+p[0];y+=+p[1];n++}}return n?[x/n,y/n]:null}
function mainland(iso){return centroid(mainRing(iso))||G.countries.get(iso)?.center||null}
function sample(r,max=320){if(!r)return[];if(r.length<=max)return r;const step=Math.ceil(r.length/max),out=[];for(let i=0;i<r.length;i+=step)out.push(r[i]);return out}
/* Same principle as the stable V51 attack origin: use the attacker's boundary nearest the target, never the country's center. */
function directionalOrigin(att,targetLon,targetLat){const r=mainRing(att),ca=mainland(att);if(!r||!ca)return null;let best=null,bd=Infinity;for(const p of sample(r)){let dx=+p[0]-targetLon;if(dx>180)dx-=360;if(dx<-180)dx+=360;const dy=+p[1]-targetLat,d=(dx*Math.cos(targetLat*Math.PI/180))**2+dy**2;if(d<bd){bd=d;best=[+p[0],+p[1]]}}if(!best)return null;return{lon:best[0]*.9+ca[0]*.1,lat:best[1]*.9+ca[1]*.1}}
async function fit(ps,s,min=650000,max=15000000){if(!ps.length)return false;const b=C.BoundingSphere.fromPoints(ps),range=Math.max(min,Math.min(max,b.radius*2.2));await new Promise(r=>G.viewer.camera.flyToBoundingSphere(b,{offset:new C.HeadingPitchRange(0,C.Math.toRadians(-89),range),duration:1.0,complete:r,cancel:r}));return s===G.navSerial}async function fly(lon,lat,h,s){await new Promise(r=>G.viewer.camera.flyTo({destination:C.Cartesian3.fromDegrees(+lon,+lat,h),orientation:{heading:0,pitch:C.Math.toRadians(-90),roll:0},duration:1,complete:r,cancel:r}));return s===G.navSerial}
function flag(iso,lon,lat,dx=24){const cc=ISO2[iso];if(!cc||!good(lon,lat))return;ents.push(G.viewer.entities.add({position:C.Cartesian3.fromDegrees(+lon,+lat,72000),billboard:{image:`https://flagcdn.com/w40/${cc}.png`,width:21,height:13,pixelOffset:new C.Cartesian2(dx,-11),disableDepthTestDistance:Number.POSITIVE_INFINITY}}))}
async function hiSwiss(){if(G.__v52SwissPrecise)return;try{const j=await fetch('https://raw.githubusercontent.com/ZHB/switzerland-geojson/master/country/switzerland.geojson',{cache:'force-cache'}).then(r=>r.json());const f=j.type==='Feature'?j:(j.features?.[0]);if(f?.geometry){const e=G.countries.get('CHE');if(e)e.feature=f;G.__v52SwissPrecise=true}}catch(e){console.warn('[V52] precise Swiss boundary unavailable',e)}}
async function country(iso,n,s){clear();if(iso==='CHE')await hiSwiss();G.flashCountry?.(iso,n);const ps=carts(iso);if(ps.length)await fit(ps,s,iso==='CHE'?430000:700000);G.__v52Trace.push({stage:'country',iso});return s===G.navSerial&&wait(1200,s)}
function chain(n){const p=n.scenePlan||{};return (Array.isArray(n.adminChain)?n.adminChain:Array.isArray(p.targetAdminChain)?p.targetAdminChain:Array.isArray(p.adminChain)?p.adminChain:[]).filter(Boolean)}async function admins(n,iso,s){for(const a of chain(n)){clear();await G.flashAdmin?.(a,iso,s,1700);G.__v52Trace.push({stage:'admin',name:a.focusLabel||a.location});if(s!==G.navSerial)return false;await wait(450,s)}return true}
async function final(n,s,flagIso=null){if(!good(n.lon,n.lat))return false;clear();const regional=/海峡|海湾|海域|湾区|群岛|半岛/.test(String(n.focusLabel||n.location||''));if(!await fly(n.lon,n.lat,regional?430000:Math.max(190000,+n.pointHeight||260000),s))return false;G.localLabelEntity=G.label?.(n.focusLabel||n.location,+n.lon,+n.lat,'country');if(flagIso)flag(flagIso,+n.lon,+n.lat,25);G.__v52Trace.push({stage:'final',lon:+n.lon,lat:+n.lat,label:n.focusLabel||n.location});return wait(2100,s)}
async function regional(n,s){const p=n.scenePlan||{},is=[String(p.primaryIso3||n.countryIso3||'').toUpperCase(),...(p.contextCountries||[]).map(x=>String(x).toUpperCase())].filter(Boolean),uniq=[...new Set(is)];clear();let ps=[];for(const x of uniq){if(x==='CHE')await hiSwiss();G.flashCountry?.(x,{...n,countryIso3:x});ps.push(...carts(x))}if(ps.length)await fit(ps,s,900000,6500000);G.__v52Trace.push({stage:'regional',countries:uniq});if(s!==G.navSerial)return false;await wait(1400,s);return final(n,s)}
async function attack(n,iso,s){const p=n.scenePlan||{},att=String(p.attackerIso3||n.sourceCountryIso3||'').toUpperCase(),vic=String(p.victimIso3||n.targetCountryIso3||n.countryIso3||iso||'').toUpperCase(),target={lon:+(n.targetLon??n.lon),lat:+(n.targetLat??n.lat)};if(!att||!vic||!good(target.lon,target.lat))return legacy(n,iso,s);clear();const src=good(n.sourceLon,n.sourceLat)?{lon:+n.sourceLon,lat:+n.sourceLat}:directionalOrigin(att,target.lon,target.lat);if(!src)return final(n,s,vic);
/* No countryStage and no pairStage: enter the actual attack area directly. */
const ps=[C.Cartesian3.fromDegrees(src.lon,src.lat,0),C.Cartesian3.fromDegrees(target.lon,target.lat,0)];await fit(ps,s,300000,2300000);if(s!==G.navSerial)return;const info={attackerIso:att,targetIso:vic,sourceLon:src.lon,sourceLat:src.lat,targetLon:target.lon,targetLat:target.lat,attackType:n.attackType,sourceType:p.sourceType||n.sourceType,sourceLabel:p.sourceLabel||`${att}方向`,targetLabel:p.targetLabel||n.focusLabel||n.location,potential:String(n.sceneMode).toUpperCase()==='POTENTIAL_ATTACK'};G.__v52Trace.push({stage:'attack-route',att,vic,source:src,target,overviewStage:false});if(info.potential)G.v29ShowPotential?.(info);else if(String(n.attackType).toLowerCase()==='drone')G.v29ShowDrone?.(n,info);else G.v29ShowMissile?.(n,info);flag(att,src.lon,src.lat);flag(vic,target.lon,target.lat);await wait(String(n.attackType).toLowerCase()==='drone'?3300:2200,s);if(s!==G.navSerial)return;return final(n,s,vic)}
G.__v52Trace=[];G.getV52Trace=()=>G.__v52Trace.slice();G.runSequence=async function(n,iso,s){G.__v52Trace.length=0;clear();iso=String(n.countryIso3||iso||'').toUpperCase();const mode=String(n.sceneMode||'').toUpperCase(),p=n.scenePlan||{};if(mode==='ATTACK'||mode==='POTENTIAL_ATTACK')return attack(n,iso,s);if(p.regionalContext)return regional(n,s);if(mode==='POINT'||mode==='ADMIN'||chain(n).length){if(!await country(iso,n,s))return;if(!await admins(n,iso,s))return;return final(n,s)}return legacy(n,iso,s)};console.info('[News Globe] V52 controller: directional attack origin, flags kept, overview removed');})();